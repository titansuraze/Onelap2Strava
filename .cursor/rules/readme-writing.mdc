---
description: README.md 写作方针 —— 读者是用户，技术细节放 FAQ 或 contexts/
globs: README.md
alwaysApply: false
---

# README 写作方针

`README.md` 的目标读者是**使用本工具的骑行爱好者**，不是代码贡献者。动它时牢记以下边界。

## 主流程段只写两件事

1. **命令**（`uv run onelap2strava ...`）
2. **期望看到的成功输出**（一小段终端摘录就行）

其他一切——内部机制、设计动机、失败路径穷举、参数的边缘用法——一律**不要**加到主流程段里。

## 技术内容的去处

| 内容类型 | 应该放在 |
|---|---|
| "为什么这样做" / "原理" | `## 常见问题` 章节的 Q&A |
| "X 失败了怎么办" / 错误消息原文解读 | `## 常见问题` 章节的 Q&A |
| 决策演进 / 废弃方案记录 | `contexts/<phase>-*.md` |
| 内部代码结构 | `## 项目结构` 章节（仅一行注释） |
| API / 实现细节 | 代码 docstring 或 `specs/*.md` |

写 FAQ 时可以展开技术细节，因为读者是**主动来查问题**的，不是第一次上手。

## 具体反例（真实删改过的内容）

以下这些是**主流程里不该出现**的写法：

- ❌ "之所以让你开这个页面，是因为它本身就是顽鹿的列表接口——下一步复制 Cookie 就从这个请求里拿..."
- ❌ "如果 Client ID 或 Secret 抄错了，会在写文件前就报错中止，例如：..." + 错误消息全文
- ❌ "`.env` 已存在时行为是'原地更新'而不是覆盖——只有三个 `STRAVA_*` 键会被替换/追加..."
- ❌ "离线 / CI 场景用 `--skip-verify` 跳过探针"（非核心路径的参数说明）

这些内容**不是**不能写，而是应该搬到 FAQ 或 contexts 里。

## 加新命令 / 改行为时的 checklist

- [ ] 主流程段给出"命令 + 成功时的输出摘录"
- [ ] 需要解释的 "why" / "if it fails" 内容，追加到 `## 常见问题` 做一条 Q&A
- [ ] 决策性内容（替代方案、权衡）更新 `contexts/*.md`，不塞进 README
- [ ] 改完后通读主流程段，确认没回归成"开发者向技术解读"
