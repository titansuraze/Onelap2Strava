---
description: Git 提交类型须按变更实质选择；禁止把代码/构建/测试改动标成 docs
alwaysApply: true
---

# Git 提交说明格式

本仓库历史提交一贯采用 **「Conventional Commit 类型 + 中文说明」**，与仅写英文摘要、或带 `scope` 括号的写法**不一致**。新建提交时对齐下面规则。

## 标题行结构

```
<类型>: <一句话说明>
```

- **类型**（英文、小写）：与常见约定一致，例如 `feat`、`fix`、`docs`、`chore`、`test`、`refactor`、`ci` 等。
- **冒号**：英文半角 `:`，后面**一个空格**再写说明。
- **说明**：用**中文**写清「做了什么 / 影响什么」。尽量一条提交一个主题；若必须带多个点，可用顿号、逗号或「，」串联，避免堆成冗长段落。

## 类型怎么选（不要默认写 docs）

**`docs` 只用于「几乎不碰产品/工具行为、只改说明性文字或文档结构」的提交**：例如 README、specs、`contexts/`、纯文档性质的 `.md`、或仓库内专门给人类看的说明。若同一提交里改了**可执行代码、配置、脚本、测试、CI**，类型必须反映**最主要的非文档变更**，不要写 `docs`。

按**实际改了什么**选类型（优先级：有代码/测试/流水线行为变化时，不要用 `docs`）：

| 实质内容 | 类型（优先选） | 备注 |
|----------|----------------|------|
| 新功能、用户可见能力、新命令/子命令 | `feat` | 含脚本里新增「产品化」能力时可算 feat |
| 缺陷修复 | `fix` | 含「修正错误行为」的配置若伴随逻辑修复 |
| 既不新增功能也非修 bug 的代码整理 | `refactor` | 行为应保持等价 |
| 性能 | `perf` | |
| 仅测试或测试基础设施 | `test` | |
| CI、Workflow、发布脚本、与构建无关的仓库维护 | `ci` / `chore` | 依团队习惯二选一，本仓库常用 `chore` |
| 依赖锁文件、包版本（无业务语义时） | `chore` | |
| **仅**文档、注释（且不改运行逻辑） | `docs` | |

**常见错误**：改了 `.ts`/`.py`/`.sh`、`package.json` 脚本逻辑、`Dockerfile`、`.github/workflows`、测试文件，却仍写 `docs:`——**一律视为错误**，应改为 `feat`/`fix`/`refactor`/`test`/`ci`/`chore` 等。

若单次提交混合多种变更：选**对用户或发布影响最大的一类**作为类型；或在拆提交无法做到时，标题说明里点出「顺带」即可，但类型仍不能写成 `docs`（除非其余改动都可忽略且确实以文档为主——这种情况应优先拆提交）。

## 刻意不用的形式

- **不要用括号 scope**，例如 ~~`docs(roadmap): ...`~~、~~`feat(api): ...`~~。本仓库的 `docs:`、`feat:` 后**直接跟中文说明**。
- **不要用**「只写泛泛英文动词、没有项目语境」作为主风格（个别极早提交如 `Create LICENSE` 可忽略，新提交不要照抄）。

## 与阶段/大功能相关的写法

里程碑或按 Phase 交付的功能，说明里直接点出阶段或范围，便于 `git log` 扫读：

- `feat: Phase 2 端到端自动同步：拉顽鹿列表 → 修坐标 → 上传 Strava`
- `docs: roadmap 反映 Phase 1/2 完成状态与实际落地偏差`（**仅当本次提交几乎只动 roadmap 等文档时**）

## 正文（可选）

需要补充上下文时，标题仍保持一行；详情用空行隔开写正文（例如复现步骤、破坏性变更说明）。本仓库多数提交仅标题即可。

## 简短对照

| 做法 | 示例 |
|------|------|
| 推荐 | `feat: strava-configure 交互命令替代手写 .env，加凭据在线校验` |
| 推荐 | `fix: 同步失败时重试逻辑未生效，修正状态判断` |
| 推荐 | `docs: roadmap 五阶段演进，新增 Phase 4 定时同步`（**纯文档**时） |
| 不推荐 | `docs: 修复上传接口超时`（改了代码却标 docs——应改为 `fix:`） |
| 不推荐 | `docs(roadmap): five phases, scheduled sync` |
