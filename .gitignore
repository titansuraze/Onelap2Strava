# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# Virtual env
.venv/
venv/
env/

# uv
.uv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets & local data
.env
data/output/
data/input/
data/.strava_token.json
data/*.json

# Test cache
.pytest_cache/
.coverage
htmlcov/

# Test fixtures contain real GPS traces (personal cycling data).
# The directory itself is tracked (via test_data/README.md) so the path
# test_fit_fixer.py references still exists; only the fit payload is kept
# out of version control. Bring your own fit files to run the regression
# test suite locally.
test_data/*.fit
